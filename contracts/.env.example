# Hedera Testnet Configuration
# Get your account from: https://portal.hedera.com/dashboard

# Required: Your Hedera account private key in HEX format (not DER format)
# Convert your private key from DER to HEX format if needed
OPERATOR_KEY_HEX="0x..."

# Optional: Custom RPC URL (defaults to testnet.hashio.io/api)
HEDERA_RPC_URL="https://testnet.hashio.io/api"

# Optional: Recipient wallet addresses for RevenueSplitter
PATIENT_WALLET="0x..."
HOSPITAL_WALLET="0x..."
MEDIPACT_WALLET="0x..."
